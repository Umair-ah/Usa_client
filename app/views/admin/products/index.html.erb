<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <%= render product %>
    <p>
      <%= link_to "Show this product", [:admin, product] %>
    </p>
    <p>
      <%= link_to  "Edit Stock", admin_product_stocks_path(product) %>
    </p>
  <% end %>
</div>

<%= link_to "New product", new_admin_product_path %>
